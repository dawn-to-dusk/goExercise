package base

import "fmt"

func Test() {
	fmt.Println(cst_a, cst_b, cst_c)
	fmt.Println(i_a, i_b, i_c, i_d, i_e, i_f, i_g, i_h, i_i, i_j)
	fmt.Println(KB, MB, GB, TB, PB)
	fmt.Println(a, b, c, d)
	fmt.Println(strs)
}
